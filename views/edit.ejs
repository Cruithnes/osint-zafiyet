<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit <%= cat.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">

  <div class="container py-5">
    <a href="/vault" class="btn btn-secondary mb-4">← Back to Vault</a>

    <div class="card mx-auto p-4 shadow-sm" style="max-width: 600px;">
      <h2 class="mb-4 text-center">Edit Cat</h2>

      <form action="/vault/<%= cat.id %>?_method=PUT" method="POST">
        <div class="mb-3">
          <label for="title" class="form-label">Cat Name</label>
          <input type="text" class="form-control" id="title" name="title" value="<%= cat.title %>" required />
        </div>

        <div class="mb-3">
          <label for="imageurl" class="form-label">Image URL</label>
          <input type="url" class="form-control" id="imageurl" name="imageurl" value="<%= cat.imageurl %>" required />
        </div>

        <div class="mb-3">
          <label for="description" class="form-label">Short Description</label>
          <textarea class="form-control" id="description" name="description" rows="3"><%= cat.description %></textarea>
        </div>

        <div class="text-center">
          <button type="submit" class="btn btn-primary">Save Changes</button>
        </div>
      </form>
    </div>
  </div>

</body>
</html>
