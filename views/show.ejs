<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= cat.name %> - Cat Vault</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">

  <div class="container py-5">
    <a href="/vault" class="btn btn-secondary mb-4">← Back to Vault</a>

    <div class="card mx-auto shadow" style="max-width: 600px;">
      <img src="<%= cat.imageurl %>" class="card-img-top" alt="<%= cat.title %>">
      <div class="card-body text-center">
        <h2 class="card-title"><%= cat.title %></h2>
        <p class="card-text text-muted mt-3">
          <%= cat.description ? cat.description : "No description available." %>
        </p>
      </div>
        <a href="/vault/<%= cat.id %>/edit" class="btn btn-outline-info">Edit Cat</a>
      <form action="/cat/<%= cat.id %>?_method=DELETE" method="POST" style="display:inline-block;">
    <button type="submit" class="btn btn-danger mt-3" onclick="return confirm('Are you sure you want to delete this cat?');">
        Delete Cat
    </button>
    </form>

    </div>
  </div>

</body>
</html>
